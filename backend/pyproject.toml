[project]
name = "crest-ai-backend"
version = "0.1.0"
description = "SOTA pre-publish AEO optimization pipeline"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I"]
